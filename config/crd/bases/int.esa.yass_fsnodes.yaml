---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: fsnodes.int.esa.yass
spec:
  group: int.esa.yass
  names:
    kind: FsNode
    listKind: FsNodeList
    plural: fsnodes
    shortNames:
    - fsns
    singular: fsnode
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .status.batteryChargeLevel
      name: BatLev
      type: string
    - jsonPath: .status.batteryChargeRate
      name: BatCharge
      type: string
    - jsonPath: .status.lowPower
      name: LowPower
      type: boolean
    - jsonPath: .status.posStr
      name: PosOverEarth
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: FsNode is the Schema for the FsNode API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FsNode
            properties:
              agent:
                description: What agent to be installed.
                properties:
                  configurationFilesFromConfigMap:
                    description: Configuration files can be mounted from ConfigMap.
                    properties:
                      configMapRef:
                        type: string
                      mountPath:
                        type: string
                    required:
                    - configMapRef
                    - mountPath
                    type: object
                  envs:
                    additionalProperties:
                      type: string
                    description: Envs environment variables
                    type: object
                  image:
                    description: Container image
                    type: string
                required:
                - image
                type: object
              earthPosition:
                description: A position of an object on the Earth.
                properties:
                  heightOverSeaLevel:
                    description: Height over sea level in meters.
                    type: number
                  lat:
                    description: Latitude in numeric format.
                    type: number
                  lng:
                    description: Longitude in numeric format.
                    type: number
                required:
                - lat
                - lng
                type: object
              engine:
                description: What file system engine to be installed
                properties:
                  configurationFilesFromConfigMap:
                    description: Configuration files can be mounted from ConfigMap.
                    properties:
                      configMapRef:
                        type: string
                      mountPath:
                        type: string
                    required:
                    - configMapRef
                    - mountPath
                    type: object
                  envs:
                    additionalProperties:
                      type: string
                    description: Envs environment variables
                    type: object
                  image:
                    description: Container image
                    type: string
                required:
                - image
                type: object
              hardwareSpec:
                description: Satellite hardware spec.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Number of available CPUs.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  diskSpace:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Available disk space. If not defined then unlimited.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Number of available RAM.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - cpu
                - memory
                type: object
              hardwareSpecRef:
                description: Satellite Hardware specification. Field hardwareSpec
                  has priority over hardwareSpecRef.
                type: string
              orbit:
                description: A position of an object as TLE.
                properties:
                  tle:
                    description: TLE lines
                    items:
                      type: string
                    maxItems: 2
                    minItems: 2
                    type: array
                required:
                - tle
                type: object
              rotation:
                description: Rotation of an object
                properties:
                  pitch:
                    type: number
                  roll:
                    type: number
                  yaw:
                    type: number
                type: object
            type: object
            x-kubernetes-validations:
            - message: Exactly one of spec.orbit or spec.earthPosition must be set
              rule: (has(self.orbit) && !has(self.earthPosition)) || (!has(self.orbit)
                && has(self.earthPosition))
            - message: At least one of spec.hardwareSpecRef or spec.hardwareSpec must
                be set
              rule: has(self.hardwareSpecRef) || has(self.hardwareSpec)
          status:
            description: status defines the observed state of FsNode
            properties:
              batteryChargeLevel:
                type: string
              batteryChargeRate:
                type: string
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lowPower:
                type: boolean
              posStr:
                type: string
              ready:
                type: boolean
            required:
            - batteryChargeLevel
            - batteryChargeRate
            - lowPower
            - posStr
            - ready
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

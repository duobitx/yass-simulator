---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: experimentdefinitions.int.esa.yass
spec:
  group: int.esa.yass
  names:
    kind: ExperimentDefinition
    listKind: ExperimentDefinitionList
    plural: experimentdefinitions
    singular: experimentdefinition
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.model
      name: Model
      type: string
    - jsonPath: .spec.maxDuration
      name: MaxDuration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: ExperimentDefinition defines behaviour and events for an experiment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          description:
            description: Description of the experiment
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExperimentDefinitionSpec defines the desired state of an
              ExperimentDefinition
            properties:
              HardwareEvents:
                items:
                  type: object
                type: array
              behaviours:
                items:
                  properties:
                    agent:
                      description: Agent on the satellite
                      properties:
                        configurationFilesFromConfigMap:
                          description: Configuration files can be mounted from ConfigMap.
                          properties:
                            configMapRef:
                              type: string
                            mountPath:
                              type: string
                          required:
                          - configMapRef
                          - mountPath
                          type: object
                        envs:
                          additionalProperties:
                            type: string
                          description: Envs environment variables
                          type: object
                        image:
                          description: Container image
                          type: string
                      required:
                      - image
                      type: object
                    fsNode:
                      description: Name of the satellite / ground station to be configured.
                      type: string
                    hardwareEvents:
                      description: What hardware events to expect during the experiment.
                      items:
                        type: object
                      type: array
                  required:
                  - agent
                  - fsNode
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - fsNode
                x-kubernetes-list-type: map
              maxDuration:
                description: MaxDuration of the experiment
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yass-web-ui
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: web-ui-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "YASS"
spec:
  ingressClassName: nginx
  rules:
  - host: {{.Values.experimentName}}-{{.Values.namespace}}.yass.duobit.pl
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-ui
            port:
              number: 80
#  tls:
#  - hosts:
#    - {{.Values.experimentName}}-{{.Values.namespace}}.yass.duobit.pl
#    secretName: yass-web-ui-tls

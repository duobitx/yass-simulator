apiVersion: v1
kind: Service
metadata:
  name: experiment-executor
spec:
  clusterIP: None  # headless service for StatefulSet
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: experiment-executor
